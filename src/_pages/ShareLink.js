import React from "react";
import { useHistory } from "react-router-dom";

const ShareLink = () => {
  const history = useHistory();

  return <div className="w-screen">{/* shared Links */}</div>;
};

export default ShareLink;
